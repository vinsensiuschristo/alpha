<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alpha">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Alpha">
        <activity
            android:name=".Fragment.ReportActivity"
            android:exported="true"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/Theme.Alpha2"/>
        <activity
            android:name=".Fragment.DetailProjectActivity"
            android:exported="true"
            android:parentActivityName=".Fragment.DetailProjectActivity"
            android:theme="@style/Theme.Alpha2"/>
        <activity
            android:name=".Fragment.ActivityDownload"
            android:exported="true"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/Theme.Alpha2"/>
        <activity
            android:name=".Fragment.ActivityHome"
            android:exported="true"
            android:parentActivityName=".HomeActivity"
            android:theme="@style/Theme.Alpha2"/>
        <activity
            android:name=".HomeActivity"
            android:theme="@style/Theme.AlphaNoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AlphaNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>